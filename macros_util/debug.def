subcode: dump_array(a)
    $foreach my $t in @$(a)
	print $t, "\n"

subcode: dump_hash(h)
    $while my ($k, $v) = each %$(h)
	print "$k: $v\n"

subcode: dump_line(name)
    my $yellow="\033[33;1m";
    my $normal="\033[0m";
    print "$yellow $(name): [$l]$normal\n"

