page: test, basic_frame
    $map test_type, i, j, k, l, m, n
    $map test_type, b, c, s

    $map test_type, pn_temp, tn_temp, ppn_temp, pppn_temp

    $struct(list) n, next
    $map test_type, list_head, t_list, p_list_head, tp_list_node

subcode: test_type(name)
    $local $(name)
    $get_type(type) $(name)
    $print type of $(name) is [$(type)]

